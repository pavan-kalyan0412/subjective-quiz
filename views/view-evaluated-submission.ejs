<!DOCTYPE html>
<html>
<head>
    <title>View Evaluated Submission</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(to right, #ffd700, #28d092);
        }

        h1 {
            text-align: center;
            margin-top: 50px;
            color: #333;
        }

        a.back-link {
            position: absolute;
            top: 10px;
            right: 10px;
            text-decoration: none;
            color: #00070e;
            font-weight: bold;
        }

        a.back-link:hover {
            text-decoration: underline;
        }

        h2, h3 {
            color: #444;
            text-align: center;
        }

        table {
            border-collapse: collapse;
            width: 80%;
            margin: 20px auto;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007BFF;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #c6f0f4;
        }

        .marks-secured {
            color: #000000;
            font-weight: bold;
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>Evaluated Submission Details</h1>
    <a class="back-link" href="/admin/view-evaluation">Back to Evaluated Submissions</a>

    <h2>User Email: <%= evaluatedSubmission.userId %></h2>
    <h3>Main Topic: <%= evaluatedSubmission.mainTopicName %></h3>
    <h3>Sub Topic: <%= evaluatedSubmission.subTopicName %></h3>

    <table border="1">
        <tr>
            <th>Question</th>
            <th>User Answer</th>
            <th>Admin Answer</th>
            <th>Admin Evaluation</th>
        </tr>
        <% evaluatedSubmission.evaluations.forEach(evaluation => { %>
            <tr>
                <td><%= evaluation.question %></td>
                <td><%= evaluation.userAnswer %></td>
                <td><%= evaluation.adminAnswer %></td>
                <td><%= evaluation.adminEvaluation %></td>
            </tr>
        <% }); %>
    </table>

    <h3 class="marks-secured">Total Marks Secured: <%= evaluatedSubmission.totalMarksSecured %></h3>
</body>
</html>
